apiVersion: v1
kind: Service
metadata:
  name: {{ include "payroll-service.fullname" . }}
  labels:
    {{- include "payroll-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "payroll-service.selectorLabels" . | nindent 4 }}
